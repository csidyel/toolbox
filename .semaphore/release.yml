version: v1.0
name: Release
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Release
    task:
      jobs:
        - name: Create and Upload assets
          commands:
            - echo "test"
